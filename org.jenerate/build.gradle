apply plugin: 'java'

repositories { 
		mavenCentral() 
}

dependencies { 
		testCompile "org.mockito:mockito-core:+" 
		testCompile "org.objenesis:objenesis:+"
}

sourceSets {
    main {
        java {
            srcDir 'src/java'
            srcDir 'src/test'
        }
    }
}